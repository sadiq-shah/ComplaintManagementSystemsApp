<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.complaintmanagementsystem">
    <uses-permission android:name="android.permission.INTERNET" />

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_icon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Home"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ProfessorRegistration" />
        <activity android:name=".StaffRegistration" />
        <activity android:name=".StudentRegistration" />
        <activity android:name=".UserRegistration" />
        <activity android:name=".Signup" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".ComplaintCategory" />
        <activity android:name=".WriteComplaint" />
        <activity android:name=".MainActivity">
        </activity>
    </application>

</manifest>